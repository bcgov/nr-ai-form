/**
 * The assistant's name, in one place.
 *
 * It appears in the launcher label, the welcome chips and the body of several canned
 * answers. Keeping it here means a rename is a single edit and the surfaces cannot
 * drift out of step with each other.
 */
export const PRODUCT_NAME = 'AIFA: AI Form Assist';

/**
 * Short form, for places where the full name does not fit - the header menu rows in
 * particular, where the card would otherwise have to stretch to hold one label.
 */
export const PRODUCT_SHORT_NAME = 'AIFA';
