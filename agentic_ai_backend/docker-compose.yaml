version: '3.8'

services:
  conversation-agent:
    build:
      context: ./agents
      dockerfile: conversationagent/Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ./agents/conversationagent/.env
    restart: unless-stopped
