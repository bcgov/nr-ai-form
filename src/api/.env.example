# Server Configuration
PORT=3000
NODE_ENV=development

# Cosmos DB Configuration
# Option 1: For local development with Cosmos DB Emulator (recommended for local dev)
COSMOS_DB_ENDPOINT=https://localhost:8081/
COSMOS_DB_DATABASE_NAME=demo-database
COSMOS_DB_CONTAINER_NAME=demo-container

# Option 2: For connecting to real Azure Cosmos DB (uncomment and replace values)
# COSMOS_DB_ENDPOINT=https://your-cosmos-account.documents.azure.com:443/
# COSMOS_DB_DATABASE_NAME=your-database-name
# COSMOS_DB_CONTAINER_NAME=demo-container

# Note: 
# - Local emulator uses a fixed key (handled automatically)
# - Azure Cosmos DB uses Managed Identity when deployed to Azure
# - For local development with real Azure Cosmos DB, ensure you're logged in: az login
